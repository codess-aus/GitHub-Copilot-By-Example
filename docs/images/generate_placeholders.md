# Generate Placeholder Hero Images

This Python script generates placeholder hero images with gradient backgrounds and text overlays.
Run this if you want to see the card layout immediately before adding custom images.

## Usage

```bash
python generate_placeholders.py
```

This will create placeholder images for all chapters in the `docs/images/` directory.
